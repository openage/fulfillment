const address = require('./addresses')
const location = require('./location')

module.exports = {
    name: 'string',
    shortName: 'string',
    code: 'string',
    contacts: ['number'],
    address: address,
    location: location
}
